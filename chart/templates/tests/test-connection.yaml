apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "djangoblog.fullname" . }}-test-connection"
  labels:
    {{- include "djangoblog.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "djangoblog.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
