apiVersion: v1
kind: Service
metadata:
  name: {{ include "djangoblog.fullname" . }}
  labels:
    {{- include "djangoblog.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "djangoblog.selectorLabels" . | nindent 4 }}
